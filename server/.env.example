# WUZAPI Manager Server - Configurações de Exemplo (Backend)
# Copie este arquivo para .env e ajuste os valores

# ============================================================================
# SERVIDOR
# ============================================================================

# Ambiente de execução (development, production)
NODE_ENV=development

# Porta do servidor
PORT=3001

# ============================================================================
# API WUZAPI EXTERNA
# ============================================================================

# URL base da API WUZAPI
WUZAPI_BASE_URL=https://wzapi.wasend.com.br

# Timeout das requisições em milissegundos
REQUEST_TIMEOUT=10000

# ============================================================================
# CORS - Origens Permitidas
# ============================================================================
# Liste todas as origens permitidas separadas por vírgula
# Desenvolvimento: inclua todas as portas do Vite
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:4173,http://localhost:8080

# ============================================================================
# ============================================================================

# Caminho do arquivo do banco de dados
# Desenvolvimento: ../data/wuzapi.db (relativo a server/)
# Docker/Produção: /app/data/wuzapi.db

# Habilitar WAL mode (OBRIGATÓRIO para performance)

# Timeout do SQLite em milissegundos

# Tamanho do cache SQLite (em páginas)

# Modo de sincronização (NORMAL, FULL, OFF)

# ============================================================================
# SESSÃO E AUTENTICAÇÃO
# ============================================================================

# Chave secreta para assinatura de cookies de sessão
# IMPORTANTE: Gerar nova chave com: openssl rand -base64 32
# NUNCA use a mesma chave em desenvolvimento e produção
SESSION_SECRET=your-secret-key-here-generate-with-openssl

# ============================================================================
# LOGGING
# ============================================================================

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# ============================================================================
# S3 STORAGE (Opcional - para armazenamento de mídia)
# ============================================================================

# Habilitar armazenamento S3 (true/false)
S3_ENABLED=false

# Endpoint S3 (AWS, DigitalOcean Spaces, MinIO, etc.)
S3_ENDPOINT=https://s3.wasend.com.br

# Região do bucket
S3_REGION=nyc3

# Nome do bucket
S3_BUCKET=megga-media

# Credenciais de acesso
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key

# Usar path-style URLs (necessário para MinIO e alguns S3-compatíveis)
S3_FORCE_PATH_STYLE=true

# Tamanho máximo de upload em bytes (50MB default)
S3_UPLOAD_MAX_SIZE=52428800

# Tempo de expiração de URLs pré-assinadas em segundos (1 hora default)
S3_PRESIGNED_URL_EXPIRY=3600

# ============================================================================
# MULTI-TENANT - Configuração de Domínios
# ============================================================================

# Domínio base para produção (sem www)
BASE_DOMAIN=cortexx.online

# Domínio base para desenvolvimento local
DEV_BASE_DOMAIN=cortexx.local

# Subdomínio do painel superadmin
SUPERADMIN_SUBDOMAIN=superadmin

# ============================================================================
# SUPABASE - Banco de Dados
# ============================================================================

# URL do projeto Supabase
SUPABASE_URL=https://seu-projeto.supabase.co

# Chave de serviço (service_role) - NUNCA exponha no frontend!
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key

# ============================================================================
# PRODUÇÃO - Configurações adicionais
# ============================================================================
# Em produção, ajuste as seguintes variáveis:
#
# NODE_ENV=production
# BASE_DOMAIN=cortexx.online
# CORS_ORIGINS=https://cortexx.online,https://*.cortexx.online
# LOG_LEVEL=info
# SESSION_SECRET=chave-segura-gerada-com-openssl
# S3_ENABLED=true
# S3_ACCESS_KEY_ID=sua-chave-de-acesso
# S3_SECRET_ACCESS_KEY=sua-chave-secreta
