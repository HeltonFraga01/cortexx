# WUZAPI Manager - Configurações de Produção
# Copie este arquivo para .env.production e ajuste os valores
# Use com: npm run build:production

# ============================================================================
# FRONTEND - Variáveis VITE_* (incorporadas no build)
# ============================================================================

# URL base da API (relativo ao mesmo servidor)
VITE_API_BASE_URL=/api

# URL da API WUZAPI externa
VITE_WUZAPI_BASE_URL=https://wzapi.wasend.com.br

# Nome e versão da aplicação
VITE_APP_NAME=WUZApi Manager
VITE_APP_VERSION=1.5.10

# ============================================================================
# BACKEND - Configurações do Servidor
# ============================================================================

# Ambiente de execução (SEMPRE production)
NODE_ENV=production

# Porta do servidor
PORT=3001

# URL da API WUZAPI externa
WUZAPI_BASE_URL=https://wzapi.wasend.com.br

# Timeout das requisições em milissegundos
REQUEST_TIMEOUT=10000

# ============================================================================
# CORS - Origens Permitidas
# ============================================================================
# Em produção, liste apenas os domínios autorizados
CORS_ORIGINS=https://seu-dominio.com

# ============================================================================
# BANCO DE DADOS SQLITE
# ============================================================================

# Caminho do arquivo do banco de dados
SQLITE_DB_PATH=/app/data/wuzapi.db

# Habilitar WAL mode (OBRIGATÓRIO para performance)
SQLITE_WAL_MODE=true

# Timeout do SQLite em milissegundos
SQLITE_TIMEOUT=5000

# Tamanho do cache SQLite (em páginas)
SQLITE_CACHE_SIZE=4000

# Modo de sincronização
SQLITE_SYNCHRONOUS=NORMAL

# ============================================================================
# SESSÃO E AUTENTICAÇÃO
# ============================================================================

# Chave secreta para assinatura de cookies de sessão
# IMPORTANTE: Gerar nova chave com: openssl rand -base64 32
# NUNCA use a mesma chave em desenvolvimento e produção
SESSION_SECRET=GERAR_CHAVE_SEGURA_COM_OPENSSL

# ============================================================================
# LOGGING
# ============================================================================

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# Formato do log (json recomendado para produção)
LOG_FORMAT=json

# Diretório de logs
LOG_DIR=/app/logs

# ============================================================================
# PERFORMANCE NODE.JS
# ============================================================================

# Opções do Node.js
NODE_OPTIONS=--max-old-space-size=512

# Número de threads do UV thread pool
UV_THREADPOOL_SIZE=4

# ============================================================================
# MONITORAMENTO (OPCIONAL)
# ============================================================================

# Habilitar métricas Prometheus
ENABLE_METRICS=true

# Intervalo de coleta de métricas em segundos
METRICS_INTERVAL=30

# ============================================================================
# BACKUP (OPCIONAL)
# ============================================================================

# Diretório de backups
BACKUP_DIR=/app/backups

# Retenção de backups em dias
BACKUP_RETENTION_DAYS=30

# Intervalo de backup em horas
BACKUP_INTERVAL_HOURS=6
